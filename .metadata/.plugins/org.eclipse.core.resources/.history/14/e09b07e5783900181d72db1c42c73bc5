package com.gammas.geoex.items;

import java.util.ArrayList;
import java.util.List;
import java.util.stream.Stream;

import com.gammas.geoc.api.ItemLists;
import com.gammas.geoc.api.RecipeHandler;
import com.gammas.geoc.api.item.IPanable;

import net.minecraft.item.Item;
import net.minecraft.item.ItemStack;
import net.minecraftforge.fml.common.Optional;

@Optional.Interface(iface = "com.gammas.geoc.api.item.IPanable", modid = "geoc")
public class ItemPanable extends BaseItem implements IPanable {

	private List<ItemStack> panningResult;
	private int panningAmount, resultAmount;
	
	public ItemPanable(String name,int panningAmount, ItemStack... panningResult) {
		super(name);
		
		this.panningResult = (List<ItemStack>) Stream.of(panningResult);
		this.panningAmount = panningAmount;
		
		RecipeHandler.panningRecipes.put(new ItemStack(this, panningAmount), this.panningResult);
		ItemLists.addCrushableItem(this);
	}

	@Optional.Method(modid = "geoc")
	@Override
	public List<ItemStack> getPanningResult() {
		return panningResult;
	}

}
